package comSpider.$6_JavaSelenium;

/*
    键盘事件:
        Keys()类提供了键盘上几乎所有按键的方法:
        使用键盘方法:
            element.sendKeys(Keys.BACK_SPACE) ==> 回格键(BackSpace)
            element.sendKeys(Keys.SPACE) ==> 空格键(BackSpace)
            element.sendKeys(Keys.TAB) ==> 制表键(Tab)
            element.sendKeys(Keys.ESCAPE) ==> 回退键(Esc)
            element.sendKeys(Keys.ENTER) ==> 回车键(Enter)
            element.sendKeys(Keys.CONTROL,'a') ==> 全选(Ctrl+A)
            element.sendKeys(Keys.CONTROL,'c') ==> 复制(Ctrl+C)
            element.sendKeys(Keys.CONTROL,'x') ==> 剪切(Ctrl+X)
            element.sendKeys(Keys.CONTROL,'v') ==> 粘贴(Ctrl+V)
            element.sendKeys(Keys.Fx) ==> F1~F12
 */

public class $3_KeyEvent {
}
